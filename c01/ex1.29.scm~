;;deps on sum
(define (integral-new a b)
  (define n 10000)
  (define h (/ (- b a) n))
  (define (term k)
    (if (or (= k 0) (= k n))
	(f (+ a (* k h)))
	(* (+ (reminder k 2) 1)
	   (f (+ a (* k h))))))
  (sum term 0 1+ n))
(integral-new 0 3)